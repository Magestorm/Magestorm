using System;
using System.Drawing;

namespace Helper
{
    public class LogMessage
    {
        public String Text;
        public Color TextColor;

        public LogMessage(String text, Color textColor)
        {
            Text = text;
            TextColor = textColor;
        }
    }
}
